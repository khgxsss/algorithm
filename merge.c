#include <stdio.h>
int number = 8;

int size;
int sorted[8]; // 정렬 배열 : 전역변수
int count = 0;

void merge(int a[], int m, int middle, int n) {
    int i = m;
    int j = middle +1;
    int k = m;

    while (i <=middle && j <= n)
    {
        if (a[i] <=a[j])
        {
            /* code */
        }
        
    }
    
}